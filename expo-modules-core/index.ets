export interface ExpoModulesRegistryExports {
  // Marker type: native modules are registered into a shared N-API `.so`.
  // The concrete per-module surfaces are declared by each module's index.d.ts.
}

export const ExpoModulesCore: ExpoModulesRegistryExports = {};
